This repository is an experiment: is it possible, using TDD, to build a fully 
functional chat without understanding how it works under the hood — and on 
top of that, in PHP, which isn't exactly suited for this kind of task?
